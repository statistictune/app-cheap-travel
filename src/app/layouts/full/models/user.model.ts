export interface LogUserModel {
    // Defina as propriedades e seus tipos correspondentes aqui
    id: string;
    email: string;
    // Outras propriedades
}

export interface GetUserModel {
    id: string;
}

export interface UserModel {
    id: string;
    name: string;
}